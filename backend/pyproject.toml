[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "beanie>=2.1.0",
    "fastapi[standard]>=0.129.0",
    "pymongo>=4.15.0",
    "python-dotenv>=1.1.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.10",
]
