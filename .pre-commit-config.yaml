files: ^backend/ # Only run on Python files in the backend directory
repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.15.10
  hooks:
    # Run the linter.
    #- id: ruff-check
    # Run the formatter.
    - id: ruff-format
      types_or: [ python, pyi ]